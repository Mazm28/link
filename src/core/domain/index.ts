/* The single definition of the domain (NFR-A2).
 *
 * Everything here is types and small pure constructors — no React, no browser
 * API, no repository. That is what lets this directory be lifted into the
 * Round-2 Node backend and, later, a React Native app without modification. */

export * from './ids';
export * from './enums';
export * from './entities';
export * from './views';
