# NFR-S5 / SECURITY-10: dependencies are pinned and the lock file is committed.
# save-exact means a new dependency is recorded without a caret range, so an
# unreviewed minor bump cannot enter the tree on a fresh install.
save-exact=true
engine-strict=true
